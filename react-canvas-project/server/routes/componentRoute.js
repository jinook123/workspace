/**
 * User(test) /component routing
 */
const express = require('express');
const mysql = require('../connection/mysqlReactConnPool');

const router = express.Router();

router.post("/test", (req, res) => {
	const sql = ``
});

module.exports = router;
